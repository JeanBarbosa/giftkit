import { styled } from '..'

export const ProfileContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',

  button: {
    margin: '1rem',
    background: 'transparent',
    border: 'none',
  }
})